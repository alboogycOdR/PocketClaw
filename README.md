# ClawCommander

Mobile AI Agent Command Centre.

A Flutter app that connects to the OpenClaw gateway and the Hermes Agent
over Tailscale, plus an on-device local-model fallback.

See `docs/` for architecture, specs, and ADRs.
